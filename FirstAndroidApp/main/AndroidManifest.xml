<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yueguo.myhw9">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DisplayDetail"
            android:parentActivityName=".MainActivity"
            android:label="Legislator Info"></activity>
        <activity android:name=".AboutMeActivity"
            android:parentActivityName=".MainActivity"
            android:label="About Me"></activity>
        <activity android:name=".DisplayBillDetail"
            android:label="Bill Info"></activity>
        <activity android:name=".DisplayCommDetail"
            android:label="Committee Info"></activity>
    </application>

</manifest>